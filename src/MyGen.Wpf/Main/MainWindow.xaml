<Window x:Class="MyGen.Wpf.Main.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyGen.Wpf.Main"
        xmlns:shared="clr-namespace:MyGen.Wpf.Shared"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="800"
        d:DataContext="{d:DesignInstance Type=local:MainViewModel, IsDesignTimeCreatable=False}">

   <Window.Resources>
      <shared:ViewTemplateSelector x:Key="templateSelector" />
   </Window.Resources>

   <Grid>
      <Grid.ColumnDefinitions>
         <ColumnDefinition Width="220" />
         <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <ContentControl Grid.Column="0" Margin="5"
                      Content="{Binding Search}"
                      ContentTemplateSelector="{StaticResource templateSelector}" />

      <TabControl Grid.Column="1" TabStripPlacement="Left" Margin="5"
                  ItemsSource="{Binding OpenTabs}"
                  SelectedItem="{Binding SelectedTab}"
                  ContentTemplateSelector="{StaticResource templateSelector}">

         <TabControl.ItemContainerStyle>
            <Style TargetType="TabItem">
               <Setter Property="HeaderTemplate">
                  <Setter.Value>
                     <DataTemplate DataType="{x:Type shared:IViewModel}">
                        <StackPanel Orientation="Horizontal" Margin="3">
                           <TextBlock Text="{Binding Path=(shared:IMainTabViewModel.Title)}" />
                        </StackPanel>
                     </DataTemplate>
                  </Setter.Value>
               </Setter>
            </Style>
         </TabControl.ItemContainerStyle>
      </TabControl>
   </Grid>
</Window>